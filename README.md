# kdp-book-automation

## ComfyUI on localhost (step-by-step)

### 1) On your local ComfyUI machine

Start ComfyUI with API enabled (default port shown):

```bash
cd /path/to/ComfyUI
python main.py --listen 127.0.0.1 --port 8188
